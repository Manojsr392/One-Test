
{
  "name": "mock-test-platform-v4",
  "version": "4.0.0",
  "description": "Banking mock-test platform with PDF import, timed tests, evaluation, ranking and analytics.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "pdf-parse": "^1.1.1"
  }
}
